# Internal Broadcom PyPI (same virtual as saltstack-raas-master). Override with:
#   export PIP_INDEX_URL=https://.../saltstack-pypi-virtual/simple
# or use pip.conf.example / scripts/setup-venv.sh
--index-url https://packages.vcfd.broadcom.net/artifactory/api/pypi/saltstack-pypi-virtual/simple

# Runtime deps (matches pyproject [project] dependencies).
mcp==1.27.0
pyyaml>=6.0.1
prometheus-client==0.25.0
starlette==1.0.0
uvicorn==0.46.0
anyio==4.13.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
sniffio>=1.3.1
httpx-sse==0.4.3
# VCF SSO / VIDB JWT authentication (T039)
PyJWT==2.12.1
cryptography==47.0.0

# Vendor SSE client (optional extra [vendor]; needs internal index with SSEApiClient):
#   PIP_INDEX_URL=.../saltstack-pypi-virtual/simple .venv/bin/pip install -e '.[vendor]'
# or install a local wheel, then pip install -e .
