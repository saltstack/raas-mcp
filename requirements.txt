# Pinned runtime dependencies (matches pyproject.toml [project] dependencies).
# Resolved from public PyPI by default. If your network requires an internal
# mirror, set PIP_INDEX_URL / use pip.conf.example before installing.
mcp==1.27.0
pyyaml>=6.0.1
prometheus-client==0.26.0
starlette==1.6.0
uvicorn==0.52.4
anyio==4.14.2
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
sniffio>=1.3.1
httpx-sse==0.4.3
# OIDC / VIDB JWT authentication
PyJWT==2.13.0
cryptography==50.0.1
